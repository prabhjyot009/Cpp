#include <iostream>
using namespace std;
class rectangle
{
    protected:
       int length;
    public:
    float breadth;
    void_get_lb()
    {
        cout<<"\nEnter the length of the rectangle";
        cin>>length;
        cout<<"\nEnter the breadth of the rectangle";
        cin>>breadth;
    }
        {
            cout<<"Enter length: ";
            cin>>length;
            cout<<"Enter breadth: ";
            cin>>breadth;
        }
        int calc()
         {
        
};
class Perimeter
{
    public:
       float calc()
        {
             return 2*(length+breadth);
        }
};
class area : public perimeter,public Rectangle   
{
    public:
    void display
{
    cout<<"Enter data for first rectangle to find area.\n";
     Area a;
     cout<<"Area = "<<a.calc()<<" square meter\n\n";
     cout<<"Enter data for second rectangle to find perimeter.\n";
     Perimeter p;
     cout<<"\nPerimeter = "<<p.calc()<<" meter";
} 
      float calc()
         {
             return length*breadth;
         }
};

int main()
{
    area obj;
    area.
     return 0;
}